class Profile:
    def __init__(self):
        return

    """
    Parses the profile contents and returns a new instance of the
    Profile class.

    Returns:
        Profile
    """
    def parse_contents(contents):
        return

    """
    Gets the contents from a profile by its name and section.

    Parameters:
        profile: The name of the profile which contents should be
        retrieved.

        use_system: A boolean value of whether to load the profile
        from the system profiles or the user profiles. If not
        specified, the profile will be loaded from the system
        profiles.

    Returns:
        String
    """
    def get_contents(profile, use_system=True):
        return

    """

    """
    def display_information():
        return

    """
    """
    def list_profiles():
        return

